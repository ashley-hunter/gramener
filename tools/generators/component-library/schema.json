{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "$id": "component-library",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the component library",
      "x-prompt": "What should the component library be called?",
      "$default": {
        "$source": "argv",
        "index": 0
      }
    }
  },
  "required": ["name"]
}
