import { Story } from '@storybook/angular';
import { <%= moduleName %> } from '<%= libraryPath %>';

export default {
  title: 'Components/<%= storyName %>'
};

interface StoryArgs {}

const DefaultStory: Story = (args: StoryArgs) => ({
  moduleMetadata: {
    imports: [<%= moduleName %>],
  },
  template: `<<%= tagName %>></<%= tagName %>>`,
  props: args,
});

export const Default = DefaultStory.bind({});

Default.args = {};
